---
all:
  children:
    base_hosts:
      hosts:
        fbsd-test:
          # For a real host set ansible_host; for the Vagrant box use `vagrant up`
          # (its ansible provisioner ignores this file). Production target: FreeBSD 15.
          ansible_host: 192.0.2.10   # replace with the test VM IP
